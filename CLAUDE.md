Read and follow `AGENTS.md` first. It is the shared source of truth for this repository's project context, workflow rules, validation commands, portfolio facts, visual design constraints, asset rules, and deployment notes.

Claude-specific note: keep implementation and review work aligned with the same constraints as Codex. Do not bypass the required validation or commit workflow documented in `AGENTS.md`.
